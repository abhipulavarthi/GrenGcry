# Development Profile Configuration

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/grengcry_dev
spring.datasource.username=postgres
spring.datasource.password=postgres

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# JWT Configuration (Use different secret for dev)
jwt.secret=DevSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong

# Logging
logging.level.com.grengcry=DEBUG
